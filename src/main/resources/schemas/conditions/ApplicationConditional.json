{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "This file will contain an allOf array with multiple if-then-else rules. The Conditional is applied on the elements of ApplicationRequest object.",
  "allOf": [
    {
      "if": {
        "properties": {
          "customerCategory": {
            "const": "Company"
          }
        },
        "required": [
          "customerCategory"
        ]
      },
      "then": {
        "required": [
          "agencyId",
          "requestType"
        ],
        "allOf": [
          {
            "not": {
              "required": [
                "address"
              ]
            },
            "x-errorMessage": {
              "not": "Property address must be omitted for customerCategory Company"
            }
          },
          {
            "not": {
              "required": [
                "individualDisclosure"
              ]
            },
            "x-errorMessage": {
              "not": "Property individualDisclosure must be omitted for customerCategory Company"
            }
          }
        ],
        "x-errorMessage": {
          "required": {
            "agencyId": "Property agencyId is required for customerCategory Company",
            "requestType": "Property requestType is required for customerCategory Company"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "customerCategory": {
            "const": "Private"
          }
        },
        "required": [
          "customerCategory"
        ]
      },
      "then": {
        "required": [
          "address",
          "individualDisclosure"
        ],
        "allOf": [
          {
            "not": {
              "required": [
                "agencyId"
              ]
            },
            "x-errorMessage": {
              "not": "Property agencyId must be omitted for customerCategory Private"
            }
          },
          {
            "not": {
              "required": [
                "requestType"
              ]
            },
            "x-errorMessage": {
              "not": "Property requestType must be omitted for customerCategory Private"
            }
          }
        ],
        "x-errorMessage": {
          "required": {
            "address": "Property address is required for customerCategory Private",
            "individualDisclosure": "Property individualDisclosure is required for customerCategory Private"
          }
        }
      }
    }
  ]
}
